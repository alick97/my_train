qsort <stdlib.h>和 sort  <algorithm> 的相关练习

qsort   的 fun 中  *(int*)p1 - *(int*)p2  表示 从小到大排序
sort    中 fun   的 int p1 < int p2 表示 从小到大排序
        中 结构体  中的 bool operator()(int a, int b) a < b  表示从小到大排序
